node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.avatar-backup

# Memory backups
memory/*.backup-*

# SQLite database files (local development data)
memory/*.db
memory/*.db-shm
memory/*.db-wal

# Environment variables with sensitive data (NEVER commit these!)
.env
.env.local
.env.production
.env.development
.env.*.local
*.env