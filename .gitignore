node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.avatar-backup

# Memory backups
memory/*.backup-*
memory/milla.db
memory/visual_memories.json

# SQLite database files (may contain sensitive data)
*.db
*.db-shm
*.db-wal

# Environment variables with sensitive data (NEVER commit these!)
.env
.env.local
.env.production
.env.development
.env.*.local
*.env